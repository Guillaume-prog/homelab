# Authentication
JWT_SECRET={{ vault_minepanel_jwt_secret }}
ALLOW_INSECURE_AUTH_COOKIES=false  # Set true only if using HTTP (LAN/dev)

# Ports (optional)
FRONTEND_PORT=3000
BACKEND_PORT=8091

# Base directory
BASE_DIR={{ config_root_path }}/minepanel

# Routing fix
FRONTEND_URL=https://minepanel.chaoshome.dev
NEXT_PUBLIC_BACKEND_URL=https://minepanel-api.chaoshome.dev
